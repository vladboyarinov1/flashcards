import { Button } from '@/components/ui/button'

export function App() {
  return (
    <div>
      <Button as={'button'} tabIndex={1} variant={'link'}>
        {/*<IconComponent />*/}
        Link wefewfwk
      </Button>
    </div>
  )
}
